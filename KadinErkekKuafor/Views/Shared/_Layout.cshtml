<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - Kuaför Yönetim Sistemi</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <style>
        /* Navbar Tasarımı */
        .navbar {
            background-color: #000; /* Siyah arka plan */
        }

            .navbar .nav-link {
                color: #fff; /* Beyaz yazı rengi */
                transition: opacity 0.3s, color 0.3s; /* Geçiş efekti */
            }

                .navbar .nav-link:hover {
                    color: #ccc; /* Üzerine gelindiğinde gri renk */
                    opacity: 1; /* Gri renge gelince opaklık kaybolmaz */
                }

            .navbar .navbar-brand {
                color: #fff; /* Marka adı beyaz */
            }

                .navbar .navbar-brand:hover {
                    color: #ccc; /* Marka adı üzerine gelince gri olur */
                }

        .btn-primary {
            background-color: #007bff; /* Varsayılan mavi */
            border: none;
        }

            .btn-primary:hover {
                opacity: 0.8; /* Hover efekti */
            }

        .btn-secondary {
            background-color: #6c757d; /* Varsayılan gri */
            border: none;
        }

            .btn-secondary:hover {
                opacity: 0.8; /* Hover efekti */
            }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Kuaför Yönetim Sistemi</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/Home/Index">Ana Sayfa</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Customer/Appointment">Randevu Al</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Customer/Services">Hizmetlerimiz</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Customer/Prices">Fiyat Listesi</a>
                    </li>
                    <li>
                        <a href="/Home/PriceList">Fiyat Listesi</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Account/Login">Oturum Aç</a>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link" href="/Admin/Dashboard">Admin Olarak Bağlan</a>
                    </li>
                   
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        @RenderBody()
    </div>

   

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>








